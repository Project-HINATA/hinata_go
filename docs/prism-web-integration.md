# PRiSM native store experience

The Flutter PRiSM module has been removed. Flutter no longer contains PRiSM pages, routes, API clients, native bridges, or localization. Types and internal method channels use `Prism` / `prism`; the App Clip target, product and display name use `PRiSM Link`; its source directory remains `ios/PrismClip`. The App Clip bundle identifier is `moe.neri.hinatago.prism`; native OAuth returns through `hinata-prism-auth`. Signing profiles and the server association file must use the same App Clip identifier.

HINATA Go (iOS/Android) and the App Clip implement the in-store experience natively. A device QR/NFC opens its store and device context, authenticates with MuNET or Passkey, verifies shop QQ membership, confirms billing when needed, and obtains the TTLock door code. Power, card and coin actions remain native. The player can review their balance, assets and visit history, redeem a code, preview checkout and leave from a focused native sheet. Account settings and merchant management remain on Web; there is no new store dashboard entry in HINATA Go.

The previous interpretation that all unified devices should redirect to Web was corrected on 2026-09-12. Current clients use device capabilities to render the native flow and do not use the legacy `webOnly` compatibility hint to redirect. Empty devices show no operation controls. Only an explicit HA `off` state gates card/coin use; unknown or failed HA reads allow use. Automatic coins hide the manual coin button. Card completion stays on the device screen with a brief success checkmark before another swipe; coin, power and door controls remain available. The server sends one coin only after successful card relay acceptance; physical game-read timing is not confirmed by that response.

The clients use `/api/v1` and `{data}` responses, with structured error codes. iOS and App Clip use the same API/model implementation. Android MuNET authorization returns through the existing `hinata-prism-auth` scheme and exchanges a one-time code into its native cookie session; only the identity provider authorization uses a browser. The full app bundle ID and `/t/:shop/:device` link shape are preserved. Both native clients derive the API origin (scheme, host and port) from the incoming HTTPS URL, including session creation, authentication, device/visit actions, images and Web links. iOS session cookies use the system persistent cookie store and follow the server’s host-only cookie scope and expiry; Flutter visit state is isolated by origin. API responses bypass URL caching. Incoming URLs with user information or invalid device identifiers are rejected. OS link associations include `link.neri.moe` and `link-beta.neri.moe`; additional domains require the corresponding platform associations to launch the app.

A second link shape, `/t/:shopCode` with no device segment, opens the shop surface instead of a machine session. `InvocationParser` treats the two shapes as unambiguous because the machine form has one extra path component; an invalid link clears the shop and fails as before. Shop mode loads only the shop and the signed-in player’s own state — no machine session, no ticket. Admission is deliberately absent: only a scanned machine ticket proves the player is at a machine, so a player who has not checked in is pointed at the machine instead. Apart from admission and machine control, the shop link offers everything a device link does.

The shop and machine links share `start(shopCode:publicId:)`, `MachineLoginView`, and a single `ClipShopHero` position in `ClipSessionPage`. Only the public resource and task area differ: a machine creates a session and loads cards; a shop loads public shop information and reads the signed-in player's summary and checkout preview together. Shop links never request machine cards or create a ticket. Both use the same sign-in buttons, account menu, QQ binding view and binding polling, error presentation, and settlement receipt. Shop information remains above sign-in after logout; private account state is cleared. A fresh URL resets its data, while another delivery of the current URL refreshes in place and preserves the receipt.

The card shows the same cover and shop name, with the billing state on the line that carries a machine name. Its width comes from the page and its image height follows the original aspect ratio; it neither crops the image nor fits it to the remaining vertical space. Image caching is shared between the two entry paths. The card stays at the same view-tree position when the task area changes from loading to bill or receipt.

The shop page owns one vertical scroll view. `ClipBillContent` contains only the shared totals and timeline, without another scroll container or an appearance-triggered request. The existing machine account sheet supplies its own scrolling and loading around that same content. Shop bill reads belong to the view model's refresh, and summary/preview publish only after both reads complete. Missing membership and disabled billing are completed states, not indefinite loading. The shop checkout button uses the shared `ClipCheckoutButton` in the page's bottom safe area; bill content is inset 24 points from the phone edge, matching the account sheet. The shop menu omits only 账单 and retains 兑换, 记录, 钱包 and 退出登录. Admission remains on machine links.

The checkout control is drawn as a transparent bottom overlay with scrollable tail padding. It does not use `safeAreaInset`, which would add an opaque system-background strip behind the button.

Checkout is a timeline, including after settlement. `player/checkout/confirm` returns the final timeline and total. The shared checkout action shows an animated circled checkmark, honors Reduce Motion, prevents duplicate submission, then dismisses the account sheet and switches to the ticket-free shop page. `ClipSettlementPage` reuses `ClipBillContent`; there is no separate line-item receipt or dismiss-to-admission button.

When no session is active, shop refresh reads `player/checkout/latest` and keeps the most recent settled timeline with “结账成功”, including after restarting the app. Active admission supersedes the old receipt; only players without a receipt see the NFC/QR admission hint. Origin/user changes and logout clear private state. This requires the corresponding PRiSM backend and migration `0027_checkout_timelines.sql`; older records use saved historical amounts, while new checkouts persist the full timeline atomically.

## Live Activity and Dynamic Island

Expanded Dynamic Island and Lock Screen activities share a bill summary: the elapsed visit time, the amount, and the earlier of the next charge or rule-switch countdown, all minute-precise (no seconds anywhere). Simultaneous events use one combined label. Compact presentation pairs the running time on the left with the countdown on the right. The optional `bill` in `StoreVisitAttributes.ContentState` is backward compatible with existing activities; all amounts arrive as integer cents and dates as Unix seconds.

Because a single session can stack several billing plans on one timeline, the activity deliberately renders no plan or rate text — the only unambiguous aggregates are the time in store, the amount, the next instant the amount can change, and cap headroom. The server may add these optional keys to `bill`, all additive and ignored by older builds. `remainingToCapCents` is the headroom to the effective cap at push time, computed by the server: with a single stacked plan, `min(global remaining, the current rule's remaining)`; with several stacked plans, the global remaining only — omit the key entirely when no global cap exists, because per-plan caps have no common aggregate once their rule switches diverge. No validity horizon is sent: every boundary that changes the aggregates (rule switches, cap-window switches, rule-anchor rollovers, charge points) is a backend alarm that pushes fresh values, so each sent value only needs to hold until the next push — the same freshness contract as the amount itself. 0 renders a "本时段已封顶" notice (segment-scoped by design: a later switch may open a fresh cap window). `billable: false` while no stacked plan is billable ("非营业") pauses the amount display and repoints the countdown at "恢复计费". `bill.nextEvent` (optional) is the single next event on the session's alarm timeline, pre-merged by the backend — every alarm candidate (charge points, rule switches, cap-window switches, billable transitions) is a peer and the earliest one wins, so the countdown is exactly the backend's next recompute-and-push; coincident candidates with different meanings arrive as one entry with a combined label. `atUnix` carries the instant and `label` the backend-rendered text (empty or missing renders a generic "下一事件"). The typed slots `nextChargeAtUnix`/`nextRuleAtUnix` were removed from the contract; builds that predate the wrapped event simply stop showing a countdown once the backend stops sending them. The legacy `planLabel` field is still accepted but no longer rendered. Layout-wise the lock-screen and expanded headers are symmetric: the leading side shows a small 在场时间 label over the running time in store (frozen total duration after checkout) with the check-in clock time in small print, and the trailing side shows the amount label over the amount with the cap notice in small print ("本时段已封顶" when capped, otherwise "距封顶还差 ¥x"). The compact presentation pairs a state icon (blue clock while metered, orange credit card while awaiting payment, gray pause while paused, green checkmark once settled) with the amount, and the minimal bubble shows the state icon alone. The countdown line shows the event's relative time with its absolute clock time in small print, and the footer carries the shop name plus the state notice. Swift renders only; it never recomputes pricing rules.

The manager requests `/player/live-activity/bill` on initial creation or stale recovery. It does not poll the endpoint on every screen refresh. Backend Durable Object alarms and business events push authoritative updates; Swift does not duplicate pricing rules. `staleDate` hides an expired countdown until new server data arrives. Closed but unpaid visits display “待结账” with a stopped duration; payment ends the activity. Ended duration is static rather than a running timer. Verify model decoding and countdown selection with `sh test/native/run-prism-activity-check.sh`; compile both Live Activity extension targets and the App Clip.

The lock-screen banner and Dynamic Island are a native-minimal presentation: a green/grey status dot, the shop name, a rounded monospaced timer, and a 轻点查看账单 hint. The activity no longer pins a black background, so it follows the system material in both light and dark. Its strings ship in a String Catalog (`ios/LiveActivityShared/Localizable.xcstrings`, en + zh-Hans) built into both widget extensions.

Tapping the activity opens `https://<origin>/t/:shopCode` through `widgetURL`, which lands on the same device-free shop surface: the native PRiSM sheet when the app or App Clip handles the link, and the web bill page in Safari otherwise. Two constraints follow from that. The activity records the HTTPS origin it was started under (link.neri.moe, the beta host, or a debug origin) and attaches no `widgetURL` when that origin is not HTTPS, so a debug loopback build cannot emit a broken link. The origin field is optional so an activity left over from an earlier build still decodes after an upgrade. The App Clip is launched by the existing `/t/` link prefix; the web fallback needs no new association.

The original App Clip invocation may be replayed when the clip is reactivated, and a Live Activity `widgetURL` is a separate explicit URL open. Both can therefore arrive within one foreground activation, in either order, and the clip's stored invocation is not replaced by the newer link. Routing keeps the delivery source so an explicit navigation cannot be overridden later in the same activation by a replayed App Clip invocation. An App Clip invocation ranks below an explicit open, because it carries the URL the clip was originally opened with and so can be stale; a Live Activity tap that carries no URL of its own reuses the link its activity was started for and ranks the same as an explicit open. The ranking is per activation and only rises, and it resets when the scene reaches the background — not on `.inactive`, which an ordinary transition passes through — so the next activation accepts a fresh invocation again. A bare `/t/{shop}` link is a legitimate invocation in its own right and is never classified by its shape. The source is taken from the system callback rather than inferred from timing, so no delay or batching is involved.

A link equal to the one already on screen refreshes in place: rebuilding there used to clear the active session and discard a settlement receipt, and it sent a shop page back to the machine page. This is separate from the ranking above and still applies. The main app defers presenting its sheet until a window exists, because a tap can arrive before the hierarchy is on screen, and it handles a URL delivered as an open rather than as a user activity.

Reads repeat once after a transient connection failure. `machines/session/start` is included in that set because a cold launch regularly loses its first request while the radio is still coming up, and it only mints a ticket: a repeated attempt leaves an unused row that expires rather than a charge. Billing and device actions stay excluded so they are never sent twice.

Device and money operations persist operation IDs per origin/user/store. Uncertain device actions are not resent. Financial retries retain their original body and ID; changing location permissions does not create a new debit request. Card sends are repeatable within a ticket. Manual and automatic coin sends share the backend device cooldown; the ticket stays usable until timeout. Account switches clear the native visit display. QQ verification and power-on poll the existing endpoints. Each operation requests native location only when the corresponding check-in, machine or checkout policy requires it. Checkout failure never ends billing; closing an app or completing a card swipe never checks out. The checkout page remains accessible after card delivery, including uncertain delivery.

Native regression checks live in the Swift/App Clip test harness. `test/native/run-prism-visit-check.sh` compiles the actual Swift models, API and App Clip view model against an isolated URLProtocol fixture plus a loopback HTTP server for persistent cookie checks; only provider authentication and location are stubbed. It checks the same flow, distinct money retry IDs after rejection, stable IDs after lost replies, and expired-session behavior after partial coin results. Standard checks are `dart format .`, `flutter analyze`, `flutter test`, the App Clip/Runner simulator builds and Android debug APK build.

For simulator inspection, Debug builds accept `PRISM_API_ORIGIN` only for loopback hosts and the App Clip accepts `PRISM_PREVIEW_DEVICE_URL` as an invocation URL. Use these with the synthetic PRiSM preview on port 8790; the real link shapes are `https://<deployment>/t/:shop/:device` for a machine session and `https://<deployment>/t/:shop` for the device-free shop page. Release builds ignore both variables. No production credentials or physical commands are used in fixture tests. Production OAuth, real NFC invocation and physical read/coin timing require device acceptance.

The App Clip follows the Web device layout. Flutter uses Material 3 independently: theme surface containers, a subtly elevated 3:2 shop banner card, 24-point page margins, 40-point hero-to-task spacing, native card ListTiles on neutral surfaces and filled/outlined actions with at least 48-point targets. Store and device names remain below the banner; missing or failed images leave the same text panel without a decorative icon or an empty image slot. Admission uses a checkbox on both native platforms. The top-right account name opens logout and, for billed shops, Bill, Redeem, History and Wallet. These open focused native sheets. The account menu remains available on the expired-session screen. There is no player shop/device list, spending page, continue-device link or automatic ticket renewal. The routed Flutter regression covers accessible targets, long-bill scrolling with a fixed checkout footer, small-screen keyboard use, QQ clipboard copying and native checkout/return navigation.

QQ binding generates its code automatically when required, polls for completion, and renews expired codes. It displays only “绑定 QQ”, “在 QQ 群中发送”, the command and expiration; Bot contact and membership explanations are omitted. Any configured device can request explicit billing admission without a door lock, using its unexpired ticket and consent. Doors additionally retrieve a temporary password and can retrieve a new password from the same page. Power progress stays inside the button; manual coin becomes disabled “已投币”, backed by the server's per-ticket atomic claim (migration 0019). Transport uncertainty expires the physical-operation UI without retrying.

Player admission rates list the enabled `entryPricing` plans, including applicable global caps. Active rules appear in priority order with weekday/date conditions, complete overnight periods, unit rates, grace and caps. Absolute date ranges are supplied in the plan's local time and labeled as continuous periods; no date picker or time-zone label is displayed.

All three clients keep 140 points/pixels of top spacing for device content, in addition to the platform safe area. The account menu stays at the top right (8 points/pixels below the safe area), independently of that content spacing. Typography and layout follow each platform rather than matching pixel sizes across clients. Flutter task headings use the regular 24-point `headlineSmall`; card rows use `titleLarge` / `bodyMedium` (22 / 14), with an 88-point minimum height and comfortable padding, and buttons inherit Material label typography. The approved shop information card retains its existing hierarchy. Task headings remain centered. Account sheets follow platform conventions: Swift uses an inline native navigation title; Flutter uses a left-aligned Material 3 `titleLarge`, a native close button and the native drag handle. On iOS 26 the account Menu uses the native glass button style and system Liquid Glass menu; older iOS versions use the native bordered capsule. Flutter uses a tonal account button opening a native Material bottom sheet with native text menu rows and a separated logout row; Web uses a translucent capsule and backdrop blur.

Admission pricing lists only rules effective on the current local date, resolved centrally by the billing timeline engine and deduplicated by rule ID while preserving their original overnight ranges, in rule-priority order, with weekdays, dates and complete overnight ranges. No date selector or time-zone label is shown. The API supplies absolute date ranges formatted in the plan time zone. Player task headings remain centered. App Clip sheets retain the default system presentation background and native inline navigation title. On iOS 26 and later, `Button(role: .close)` uses the trailing navigation toolbar’s native glass background, with a neutral foreground. Do not add `.buttonStyle(.glass)` or a custom button shape/size inside this toolbar: that stacks a second glass control over its shared background. The sheet uses the system corner radius; there is no fixed corner or custom close-button inset. Earlier systems use a toolbar xmark button. Checkout stays in a bottom safe-area inset outside the scrolling bill. Flutter keeps its total, checkout action and header outside the scrolling content, including long bills and keyboard resizing. Sheet dates follow the app locale; financial values use tabular figures. Redeem uses a native filled text field. All Flutter surfaces follow the active light/dark or Android dynamic color scheme, without simulated glass. Do not infer API use from glass highlights in a still image or replace the sheet background to force a particular appearance. Current simulator captures use iOS 26.3.1; the user's Apple Maps reference uses iOS 27, whose rendering has not been validated locally.

Redeem remains disabled until a non-empty code is entered, matching Web and App Clip.

Flutter design references: [M3 lists](https://m3.material.io/components/lists/guidelines), [menus](https://m3.material.io/components/menus/guidelines), [bottom sheets](https://m3.material.io/components/bottom-sheets/guidelines), [buttons](https://m3.material.io/components/buttons/guidelines), and [Flutter text styles](https://api.flutter.dev/flutter/material/TextTheme-class.html). Mobile menus may adapt to bottom sheets. A card tap here is a single action, not a persistent radio selection; it retains the existing immediate login behavior. Ordinary account choices use list rows instead of repeated tonal buttons. Component shape and text roles come from Material; visual parity with Swift/Web is not a requirement.

Swift player task headings use the Dynamic Type-aware `.title2` style rather than a fixed 32-point bold font. Web follows the same default visual hierarchy with 1.375rem regular task headings and its existing system font stack. The shop information card is unchanged.

Manual coin uses a primary theme-filled Material button with in-button progress. It remains usable after success; the backend enforces the device cooldown.

Native clients prefer the unified `locationEnabled` policy and fall back to legacy flags. They collect fresh coordinates for enabled entry/device/checkout requests; the API requires being inside for venue operations and outside for checkout. Poor or missing fixes retain the active visit.

Card success does not start a delayed redirect. The close toolbar item also uses an iOS 15-compatible ViewBuilder condition.

App Clip status polling pauses while the scene is inactive and resumes with a silent status refresh. A timer releases its task reference before refreshing, so the refresh cannot cancel itself. Transient network failures during automatic reads retry without an alert; cancelled or stale reads are ignored. Foreground recovery preserves the device ticket and never replays card, coin or financial actions.

App Clip and the full iOS app retain native login cookies across API client recreation and normal relaunch through `URLSessionConfiguration.default`. Tests can still inject ephemeral configuration. Sign-out removes the cookie through the existing server response; no account token is copied between hosts. The system may remove App Clip data when reclaiming an unused clip, so that case still requires signing in again.

Account sheets fetch only their selected content: Bill reads summary plus checkout preview; History and Wallet each read one batch only when opened; Redeem needs no initial request. Device refresh does not read assets or history. The account button shows a compact billing indicator while the summary has an active session, and clears it after checkout. Read-only transport failures receive one retry (GET or checkout preview); financial and physical mutations are never automatically resent. Sheet read errors remain inline with a retry action. Android login cookies remain in memory; the persistent-cookie change applies to iOS/App Clip.

Only ticket expiry routes to the expired page. IO failures show the server device error, or a network error for client transport failures, on the current screen. Internal duplicate-dispatch protection remains, but does not expose a “result pending confirmation” state or expire the ticket. Initial native device/power reads share the card-loading phase, with no refresh-state button flashing during load.

麻将区域使用紧凑的状态／人数标题行和座位网格，展示空位与本人标记。满桌且本人未上桌时展示状态文字，不再显示停用的大按钮；保留店铺 banner。Flutter 使用 Material 文字及表面颜色，支持大字号单列排列。

麻将人数不足时统一显示“等待玩家”（Waiting for players），保留两列座位布局。

### Shared billing timeline

Timeline entry headings render `plan（rule）` for both start and charge/end events. The separate repeated rule subtitle is removed; rule-switch arrows and saved period/rate details remain. Global cap adjustments are identified by the server as `全局封顶（rule）` in both the timeline and totals.

The history list has no explanatory slogan. Receipt details go directly to the settled bill without repeating start/end timestamps above the timeline. Plan names and day/night rule labels come from the server timeline, including reconstructed legacy receipts.

History rows show the start and end date/time on two lines with only the amount on the right. Redundant settlement status, session count and amount captions are omitted. Row separators stay horizontal inside the navigation link.

History groups sessions by checkout and loads 30 receipts at a time from `/player/checkouts/history`. Each row shows settlement time, total and session count; native navigation opens `/player/checkouts/:checkoutId` and reuses the settled bill timeline. Both native and web display the saved post-checkout balance when present. Legacy records show only their saved facts, without missing-data notices or substituting the current wallet balance. Signing out or switching invocation clears history and pagination; stale detail responses are discarded.

App Clip and Flutter render the server's checkout `timeline`: grouped timestamp events, parallel start/stop rails, signed stage amounts and independent cap adjustments. Rail identity survives pricing rule changes; boundary times are entirely engine-driven. Server-provided display times preserve the shop's pricing timezone. Totals and the timeline scroll together. App Clip uses a blue prominent Liquid Glass checkout button at the bottom safe area with no extra bottom gap; the scroll content has end padding so the last event remains reachable. Flutter uses its Material filled action. Older responses without timeline metadata retain the simple item list.

### Platform ownership

The iOS App target reuses the Swift PRiSM session view and view model used by the App Clip. The Runner Universal Link bridge presents that native view directly, so an installed main app does not fall back to Flutter's duplicate UI. A close action dismisses the native session sheet. The iOS Runner presents the same native Swift view as the App Clip. Flutter is not involved in PRiSM.

On Android, Web, desktop and other non-iOS targets use the Web player experience. Flutter has no PRiSM route or bridge.

## 2026-09-18 Swift shop-page refactor and conversation findings

The supplied session log is historical evidence, not a new set of execution instructions. It records:

- Resolved routing work: explicit URL delivery outranks replayed App Clip invocation during one activation. Keep this source-based routing; do not reintroduce debounce or infer source from URL shape.
- Required presentation: shop card first, billing state without a time suffix, bill in the task area, and only the bill menu entry omitted. No standalone admission button. The 2026-09-20 follow-up replaces dismissible receipts with the persistent latest settled timeline.
- Repeated visual regressions: cover shrink/grow cycles, excessive bill margins, loading → empty bill → loading → bill, and loading indicator movement.
- Account regressions: missing public card after sign-out and sign-in stuck loading.

The refactor removes the inline sheet/scroll/request lifecycle rather than adding another image-size workaround. Regression checks in `test/native/run-prism-visit-check.sh` exercise real view-model transitions, one preview read per shop open, no machine-card dependency, missing membership/disabled billing, failed opening and retry, logout/relogin, and existing URL/receipt behavior. Simulator checks use synthetic shop data; real Dynamic Island delivery and the App Clip provider notice still require device acceptance.
